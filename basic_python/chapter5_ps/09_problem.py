# Can you change the values inside a list which is contained in set S?
s = {8, 7, 12, "Harry", [1,2]}
# no!......we can not do this because set is immutable and mane part of this set
# a list can define in set